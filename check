when(entityManager.createNativeQuery(anyString())).thenReturn(query);
when(entityManager.createNativeQuery(anyString(), eq(ArrestInfo.class))).thenReturn(query);

=======
There are 7 tests in that class — do a find/replace within the GetArrestInfoSummaryListTests class:

Find:    when(entityManager.createNativeQuery(anyString())).thenReturn(query);
Replace: when(entityManager.createNativeQuery(anyString(), eq(ArrestInfo.class))).thenReturn(query);

