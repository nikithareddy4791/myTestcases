@Test
@WithMockUser
void deleteCaseUploadById_objectMapperThrows_returns500() throws Exception {
    doNothing().when(caseService).deleteFile(100, 1);
    doThrow(new IOException("Serialization error"))
            .when(objectMapper).readValue(anyString(), eq(String.class));

    mockMvc.perform(delete("/case/100/file/1")
            .with(csrf())
            .accept(MediaType.APPLICATION_JSON))
            .andExpect(status().isInternalServerError());
}
