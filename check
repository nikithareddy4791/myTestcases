// Line 114 — was:
DDDCaseStats stats = caseService.getCaseStats();
// Fix:
DDDCaseStats stats = caseService.getCaseStats((String) null);

// Line 128 — was:
DDDCaseStats stats = caseService.getCaseStats();
// Fix:
DDDCaseStats stats = caseService.getCaseStats((String) null);

// Line 145 — was:
DDDCaseStats stats = caseService.getCaseStats(officeIds);
// Fix:
DDDCaseStats stats = caseService.getCaseStats(officeIds, "jdoe");
