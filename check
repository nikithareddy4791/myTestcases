  public void clearCache(final String cacheName)
    {
        
1
2
cacheManager.getCache(cacheName).clear();
A "NullPointerException" could be thrown; "getCache()" can return null.
    }
}



public class PrinthubUtil {
    private static final String PRINTHUB_HOST = "https://printhub.nypd.org";
    private static SimpleDateFormat dateFormat = new SimpleDateFormat("MMddyyyy");
Make "dateFormat" an instance variable.
    @Autowired
    private AuthenticationService authenticationService;
    public String getReportUrl(String arrId) {
Uncovered code
        String tax = authenticationService.getTaxId();
        StringBuilder printhubURL = new StringBuilder(PRINTHUB_HOST);
        final String url = "/report/omni/arrest/" + tax + "/" + arrId;
        final String hashKey = getGeneratedSha256Hash(url);
